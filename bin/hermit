#!/usr/bin/env python

from hermit.ui import main


if __name__ == '__main__':
    try:
        main()
    except TypeError as e:
        # FIXME why does this error even occur?
        if 'a coroutine or an awaitable is required' in str(e):
            pass
        else:
            raise e
