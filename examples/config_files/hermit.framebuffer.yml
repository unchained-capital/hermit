---

# This configuration file specifies that Hermit should run in the
# framebuffer display mode.
#
# Run Hermit using this file via:
#
#   $ HERMIT_CONFIG=examples/config_files/hermit.framebuffer.yml
#

io:
  display: framebuffer
